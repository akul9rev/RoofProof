{
  "name": "roofproof-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "RoofProof: Privacy-preserving rental verification DApp on the Midnight Network",
  "workspaces": [
    "apps/frontend",
    "apps/backend",
    "packages/contracts"
  ],
  "scripts": {
    "build": "npm --prefix apps/frontend install && npm --prefix apps/frontend run build && npm --prefix apps/backend install",
    "start": "node apps/backend/src/server.js",
    "dev:frontend": "npm --prefix apps/frontend run dev",
    "dev:backend": "npm --prefix apps/backend run dev",
    "db:migrate": "node apps/backend/src/db/migrate.js",
    "db:seed": "node apps/backend/src/db/seed.js",
    "test": "echo \"Error: no test specified\" && exit 0"
  }
}
